# AGENTS.md

이 파일은 이 저장소에서 팀과 AI 에이전트가 함께 작업할 때 따르는 공통 기준이다. 특정 기능이나 도메인에 종속되지 않으며, 모든 변경은 현재 저장소의 구조와 GitHub 협업 규칙을 기준으로 수행한다.

## 기준과 우선순위

- 사용자 요청을 가장 우선한다.
- 저장소 내부의 더 가까운 위치에 별도 `AGENTS.md`가 있으면 해당 범위에서는 그 지침을 우선한다.
- 협업 절차는 `.github`의 이슈·PR 템플릿, 워크플로와 `CONTRIBUTING.md`를 따른다.
- 구현 방식은 `package.json`, 설정 파일, 기존 코드 패턴을 근거로 결정한다.
- 지침이 충돌하거나 요구사항의 핵심이 불명확하면 임의로 결정하지 말고 확인한다.

## 기본 작업 원칙

- 답변, 진행 상황, PR 설명과 리뷰 답글은 한국어로 작성한다.
- 코드, 변수명, 함수명, 클래스명, 파일명은 영어를 사용한다.
- 작업 전 요구사항, 영향 범위와 관련 코드를 확인한다.
- 추측보다 파일 내용, 검색 결과, diff와 명령 결과를 근거로 판단한다.
- 기존 아키텍처, 네이밍, 코드 스타일을 우선하며 필요한 범위만 변경한다.
- 요청과 관계없는 리팩터링이나 포맷 변경을 섞지 않는다.
- 사용자 또는 다른 팀원의 변경을 되돌리거나 덮어쓰지 않는다.
- 새로운 의존성, 전역 설정 변경, 공개 API 변경은 필요성과 영향을 먼저 확인한다.
- 임시 코드, 디버깅 로그, 사용하지 않는 import와 주석은 완료 전에 제거한다.
- 실제 동작하지 않는 정적 데이터나 임시 우회로 작업 완료를 가장하지 않는다.

## 작업 절차

1. 요청과 완료 조건을 정리한다.
2. 관련 파일과 기존 구현 패턴을 탐색한다.
3. 변경 범위와 의존 관계를 확인한다.
4. 작은 단위로 구현하고 변경 diff를 점검한다.
5. 오류, 로딩, 빈 상태와 경계 조건을 확인한다.
6. 변경 범위에 맞는 lint, build와 수동 검증을 수행한다.
7. 변경 내용, 검증 결과와 남은 위험을 요약한다.

## 프로젝트 기술 기준

- Vue 3 Composition API와 `<script setup>`
- JavaScript ES Modules와 `.vue` SFC
- Vite 7, Vue Router 4, Pinia 3, TanStack Vue Query 5
- Axios, Zod와 Tailwind CSS 4
- npm과 `package-lock.json`
- ESLint, Prettier와 Lefthook
- CI 실행 환경 Node.js 24

- TypeScript는 별도 합의 없이 새로 도입하지 않는다.
- 패키지는 npm으로 관리하고 의존성 설치 시 lockfile을 함께 유지한다.
- 실제 설치 버전과 명령은 항상 `package.json`과 lockfile을 기준으로 한다.

## 저장소 구조

```text
.
├── .github/{ISSUE_TEMPLATE,PULL_REQUEST_TEMPLATE.md,workflows/ci.yml}
├── src/
│   ├── api/
│   ├── assets/
│   ├── components/
│   ├── composables/
│   ├── constants/
│   ├── router/
│   ├── shared/ui/
│   ├── stores/
│   ├── utils/
│   └── views/
├── package.json
├── package-lock.json
├── vite.config.js
├── eslint.config.js
└── jsconfig.json
```

- 페이지 단위 화면은 `src/views`에 둔다.
- 여러 화면에서 재사용하는 범용 UI는 `src/shared/ui`에 두고 `index.js`에서 공개한다.
- 기능 중심 컴포넌트는 `src/components`에 둔다.
- 재사용 상태 로직은 `src/composables`, 전역 클라이언트 상태는 `src/stores`에 둔다.
- API 인스턴스와 요청 함수는 `src/api`, 라우트 설정은 `src/router`에 둔다.
- 공통 상수와 순수 유틸리티는 각각 `src/constants`, `src/utils`에 둔다.
- 기존 역할에 맞는 폴더가 있으면 새 폴더를 만들기보다 그 구조를 재사용한다.

## JavaScript와 Vue 규칙

- 변수명과 함수명은 camelCase, 컴포넌트명과 클래스명은 PascalCase를 사용한다.
- 컴포넌트 파일명은 PascalCase로 작성하며, 의미가 명확하면 단일어도 허용한다.
- 의미가 불분명한 이름과 불필요한 축약어를 피한다.
- 문자열, 세미콜론과 줄 길이는 ESLint와 Prettier 설정을 따른다.
- import는 `@/` alias를 우선하고 같은 영역의 짧은 상대경로는 기존 패턴을 따른다.
- 컴포넌트는 Composition API와 기존 `<script setup>` 방식을 유지한다.
- `props`와 `emits`의 이름, 기본값과 필수 여부를 명확히 한다.
- `v-for`에는 안정적인 고유 `key`를 사용한다.
- `v-if`와 `v-for`를 같은 요소에 함께 사용하지 않는다.
- 복잡한 계산과 부수 효과를 템플릿에 직접 작성하지 않는다.
- 파생 값은 `computed`, 재사용 로직은 `use` 접두사의 composable로 분리한다.
- 페이지는 데이터 조회와 화면 조합을 담당하고 표시 전용 컴포넌트는 API를 직접 호출하지 않는다.
- 주석은 코드만으로 의도가 드러나지 않는 이유와 제약을 설명할 때만 한국어로 작성한다.

## 상태 관리와 API 연동

- 화면 내부에서만 쓰는 값은 로컬 상태로 관리한다.
- 여러 컴포넌트가 공유하는 클라이언트 상태만 Pinia에 둔다.
- 기존 `src/api/client.js`의 Axios 인스턴스와 `/api` 프록시를 재사용한다.
- Base URL, 인증 헤더와 공통 오류 처리를 컴포넌트에 중복 작성하지 않는다.
- 페이지와 표시 컴포넌트에서 Axios를 직접 호출하지 않고 API 모듈을 거친다.
- API 응답 필드를 임의로 바꾸거나 누락 필드를 추측해서 추가하지 않는다.
- 서버 오류를 빈 데이터나 성공 상태로 숨기지 않는다.
- 조회 화면은 필요한 경우 로딩, 빈 상태와 오류 상태를 구분한다.
- 생성·수정·삭제 요청 중에는 중복 실행을 방지하고 성공 후 관련 캐시를 갱신한다.
- 인증 정보와 민감한 값은 코드, Store 또는 `localStorage`에 새로 저장하지 않는다.
- Zod 스키마를 사용할 때는 실제 API 경계에서 검증하고 실패를 명시적으로 처리한다.

## UI와 접근성

- 기존 `src/shared/ui` 컴포넌트와 디자인 패턴을 먼저 재사용한다.
- 공통 컴포넌트는 특정 페이지의 비즈니스 로직에 의존하지 않게 유지한다.
- Tailwind CSS와 기존 디자인 토큰을 사용하고 유사 스타일을 중복 정의하지 않는다.
- 버튼, 입력, 대화상자에는 의미에 맞는 HTML 요소와 접근 가능한 이름을 사용한다.
- 키보드 조작, 포커스 이동, disabled 상태와 오류 안내를 함께 고려한다.
- 반응형 화면은 기존 breakpoint와 레이아웃 패턴을 따른다.
- 이미지와 아이콘에는 용도에 맞는 대체 텍스트 또는 접근성 제외 처리를 적용한다.

## 보안과 안정성

- 토큰, 비밀번호, 개인 정보와 실제 환경 변수 값을 커밋하지 않는다.
- `.env.example`에는 비밀이 아닌 예시 값만 둔다.
- 사용자 입력은 신뢰하지 않고 출력 위치와 API 경계에 맞게 검증한다.
- 위험한 HTML 주입과 민감 정보가 포함된 로그를 피한다.
- 라우트 파라미터, 쿼리와 외부 응답이 없거나 잘못된 경우를 처리한다.
- 삭제나 되돌리기처럼 복구가 어려운 작업은 정확한 대상과 사용자 의도를 먼저 확인한다.

## 검증과 완료 조건

- 변경 파일의 diff를 확인해 의도하지 않은 수정이 없는지 점검한다.
- CI와 동일한 기본 검증을 실행한다.

```bash
npm ci
npx eslint .
npm run build
```

- 의존성이 이미 설치된 로컬 환경에서는 `npm ci`를 생략할 수 있다.
- PR 전에는 `CONTRIBUTING.md`와 PR 체크리스트에 따라 `npm run lint`와 `npm run build`를 실행한다. `npm run lint`는 `--fix`를 포함하므로 실행 후 diff를 확인한다.
- 별도 테스트 스크립트가 추가되면 변경 범위와 관련된 테스트를 함께 실행한다.
- UI 변경은 주요 상태와 사용자 흐름을 브라우저에서 확인한다.
- 검증을 실행하지 못했거나 실패했다면 원인과 영향 범위를 숨기지 않고 보고한다.

## GitHub 협업 규칙

- 모든 구현·수정 작업은 연결된 GitHub 이슈의 범위와 완료 조건을 먼저 확인한다.
- 연결할 이슈가 없으면 목적에 맞는 `.github/ISSUE_TEMPLATE`으로 새 이슈를 작성한 뒤 작업을 시작하며, 하나의 이슈에는 하나의 목적만 담는다.
- 작업 브랜치의 분기 기준과 작업 단위는 `CONTRIBUTING.md`의 브랜치 전략을 따른다.
- 브랜치명과 작업 타입은 `CONTRIBUTING.md`의 네이밍 규칙을 따르며, 브랜치 설명은 영어 소문자 케밥 케이스로 작성한다.
- 커밋은 `<type>: <subject>` 형식의 Conventional Commits를 따른다.
- 커밋에는 관련 변경만 담고 생성물, 비밀값과 불필요한 포맷 변경을 포함하지 않는다.
- PR의 기본 대상은 `develop`이며 제목은 `[#이슈번호] type: 작업 내용` 형식을 사용한다.
- `.github/PULL_REQUEST_TEMPLATE.md`를 유지하고 관련 이슈, 작업 내용, 변경 유형과 검증 결과를 채운다.
- PR 전 자체 리뷰와 CI 검증을 마치고, UI 변경은 필요한 경우 스크린샷을 첨부한다.
- 최소 1명의 승인 후 Squash and Merge를 사용한다.
- 리뷰 의견에는 반영 결과 또는 반영하지 않은 이유를 답하고, 선택 의견은 `nit:`로 구분한다.
- 요청 없이 커밋, push, 브랜치 생성, PR 생성 또는 병합을 수행하지 않는다.

## 작업 결과 보고

- 핵심 변경 사항과 영향을 받은 영역을 간결하게 설명한다.
- 실행한 검증 명령과 결과를 정확히 기록한다.
- 미검증 항목, 알려진 제약과 후속 작업이 있으면 명시한다.
- 파일을 변경하지 않은 조사 작업은 확인한 근거와 결론만 보고한다.
